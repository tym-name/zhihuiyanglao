1. 恢复 `http` 实例的导入，用于发送HTTP请求
2. 实现 `handleUpload` 函数，处理图片上传逻辑：

   * 创建FormData对象，添加要上传的文件

   * 调用http.post上传文件到服务器获取URL

   * 调用uploadAdd接口保存图片信息

   * 上传成功后更新表单数据和文件列表
3. 为 `el-upload` 组件添加 `:http-request="handleUpload"` 属性
4. 确保上传失败时显示错误信息
5. 测试图片上传功能是否正常工作

